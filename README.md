# Arsenal Internal Injector

**please do not use this to just ruin the game it is made for just pranks.**

This tool allows direct manipulation of Roblox Arsenal client memory. Built with Python and uses PyGetWindow + PIL for interaction.

### Features:
- Gravity modification (VIP only)
- Aimbot module (in dev)
- ESP (in dev)

### Setup:
```bash
pip install -r requirements.txt
python main.py
